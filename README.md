# RPG Master Plan

App desktop (Electron) de planejamento de sessões de RPG. Ver `SPEC.md` para a especificação e `CLAUDE.md` para o schema dos dados.

## Desenvolvimento

```
npm install
npm run dev      # electron-vite dev
npm run build    # tsc -b + electron-vite build
npm run lint     # oxlint
```
